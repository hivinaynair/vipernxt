export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl items-center px-6">
      <section className="space-y-3">
        <h1 className="text-3xl font-semibold">Ready for the first slice</h1>
        <p className="text-muted-foreground">
          Build the approved journey here. The stack is configured; product behavior still needs
          verification.
        </p>
      </section>
    </main>
  );
}
